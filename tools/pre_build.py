Import("env")

# pre_build.py – placeholder pre-build script
# Extend here if you need to auto-generate version headers,
# fetch secrets, or run other pre-compile tasks.

print("[pre_build] ESP32 DC Motor Controller – pre-build step OK")
